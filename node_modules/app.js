const express = require("express")
const mongoos = require("mongoose")
const app = express()

mongoos.connect("데이터베이스 주소", {
    userNewUrlParser: true,
    useUnifiedTopology: true,
}).then(()=>console.log("connected to database"));

module.exports= app